import React, { useEffect, useState } from 'react' ;

const ClaseUnouseEffect = () => { 

    {/* 
    const [ luzPrendida , setLuzPrendida] = useState( true );
    const [ colorDePieza , setColorDePieza ] = useState ('blanco') ;

    useEffect( ()=>{
        if( luzPrendida){
            setColorDePieza("Rojo Encendido")
        }else{
            setColorDePieza('azul muy oscuro')
        }
    } , [ ])

    return (
        <div>
            <button onClick={ () => setLuzPrendida( !luzPrendida )}> 
                { luzPrendida ? 'apagar la luz' : 'prender la luz'} 
            </button>

            <p> El cuarto esta pintado de color :  { colorDePieza } </p>

    </div>}*/}

   {/* const [ haySietes , setHaySietes ] = useState( true ) ;
    const [ resultadoBoquita , setResultadoBoquita ] = useState("Se juega la semi")

    useEffect( ()=>{
        if( haySietes ){
            setResultadoBoquita( "le hacemos 7 al palmeiras")
        }else{
            setResultadoBoquita( " siamo fuori de la copa insta")
        }
    },[ resultadoBoquita ])

    return(
      <div> 
        <button onClick={ () => setHaySietes( !haySietes) }> { haySietes ? 'Tranqui te confirmo que ' : 'Busca siestes porque sino'} </button>
        <p> tenemos el agrado de decirte que : { resultadoBoquita } </p>
      </div>
    )*/} 
  
 
} ;

export default ClaseUnouseEffect ;